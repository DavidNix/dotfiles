---
description: Review security using the security-review skill
---

Load the `security-review` skill and follow its instructions exactly. Use the following as `$ARGUMENTS` for the skill:

$ARGUMENTS
