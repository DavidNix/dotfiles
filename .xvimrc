" note: there is no way to set leader

nnoremap ; :

set number

" get back to normal mode quickly
inoremap jk <Esc> 

" window navigation
map <C-j> <C-w>j<CR>
map <C-k> <C-w>k<CR>
map <C-h> <C-w>h<CR>
map <C-l> <C-w>l<CR>

" don't show file status bar
set laststatus=0

" case-insensitive searching
set ignorecase
set smartcase

" Use system pasteboard
set clipboard=unnamed

" mimic vim for comments
nnoremap gcc :xccmd commentAndUncommentCurrentLines<CR>
vnoremap gcc :xccmd commentAndUncommentCurrentLines<CR>

" breakpoints
map ,b :xccmd toggleBreakpointAtCurrentLine<CR>

" file navigation forward and back
map L :xccmd goForwardInHistoryByCommand<CR>
map H :xccmd goBackInHistoryByCommand<CR>

" switch between .h/.m
map J :xccmd jumpToNextCounterpart<CR> 
map K :xccmd jumpToNextCounterpart<CR> 

" reveal file or symbol
map ,pr :xccmd revealInProjectNavigator<CR>
