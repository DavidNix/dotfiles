#!/usr/bin/env bash
# Run OpenCode in an isolated Apple Container environment.

set -euo pipefail

SANDBOX_IMAGE="ai-sandbox:latest"
DOCKERFILE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

log_info() { printf '%b[INFO]%b %s\n' "$BLUE" "$NC" "$*"; }
log_success() { printf '%b[OK]%b %s\n' "$GREEN" "$NC" "$*"; }
log_warn() { printf '%b[WARN]%b %s\n' "$YELLOW" "$NC" "$*"; }
log_error() { printf '%b[ERROR]%b %s\n' "$RED" "$NC" "$*" >&2; }

usage() {
    cat <<EOF
Usage: ai-sandbox [build|reset|prune|shell] [--] [OPENCODE_ARGS...]

Run OpenCode without external plugins in a persistent Apple Container sandbox
for the current directory.

COMMANDS:
    build       Build/rebuild the sandbox image
    reset       Remove the sandbox for the current directory
    prune       Delete all ai-sandbox containers
    shell       Open an interactive shell inside the sandbox

With no command, ai-sandbox starts OpenCode in the current directory.

OPTIONS:
    -h, --help  Show this help message

EXAMPLES:
    ai-sandbox                  # Run opencode in the sandbox
    ai-sandbox --help           # Show this help message
    ai-sandbox -- --help        # Pass --help to opencode
    ai-sandbox build --no-cache # Rebuild from scratch
    ai-sandbox reset            # Remove the current sandbox

EOF
}

ensure_system_running() {
    if ! container ls >/dev/null 2>&1; then
        log_info "Starting container system..."
        if [[ -t 0 ]]; then
            container system start
        else
            printf 'Y\n' | container system start
        fi
        sleep 2
    fi
}

build_image() {
    local force="${1:-}"
    local no_cache="${2:-}"

    if [[ -z "$force" ]] && image_exists "$SANDBOX_IMAGE"; then
        return 0
    fi

    local host_home host_uid host_gid
    host_home="$HOME"
    host_uid=$(id -u)
    host_gid=$(id -g)

    log_info "Building sandbox image..."

    local build_args=(
        -f "${DOCKERFILE_DIR}/Dockerfile.ai-sandbox"
        -t "$SANDBOX_IMAGE"
        --build-arg "HOST_HOME=${host_home}"
        --build-arg "HOST_UID=${host_uid}"
        --build-arg "HOST_GID=${host_gid}"
    )
    if [[ -n "$no_cache" ]]; then
        build_args+=(--no-cache)
    fi
    build_args+=("${DOCKERFILE_DIR}")

    run_container_build "${build_args[@]}"
    log_success "Image built successfully"
}

image_exists() {
    local image="$1"
    local name tag

    name="${image%%:*}"
    tag="${image#*:}"
    [[ "$tag" != "$image" ]] || tag="latest"

    container image list 2>/dev/null | awk -v name="$name" -v tag="$tag" '
        NR > 1 && $1 == name && $2 == tag { found = 1 }
        END { exit found ? 0 : 1 }
    '
}

run_container_build() {
    local log_file status

    log_file=$(mktemp)
    set +e
    container build "$@" > >(tee "$log_file") 2>&1
    status=$?
    set -e

    if [[ $status -eq 0 ]]; then
        rm -f "$log_file"
        return 0
    fi

    if grep -q 'default kernel not configured' "$log_file"; then
        rm -f "$log_file"
        log_warn "No default Apple Container kernel configured; installing the recommended kernel..."
        container system kernel set --recommended --force
        container build "$@"
        return $?
    fi

    rm -f "$log_file"
    return "$status"
}

container_name_from_dir() {
    local dir="$1"
    local base_name safe_name hash

    base_name=$(basename "$dir")
    safe_name=$(printf '%s' "$base_name" | tr '[:upper:]' '[:lower:]' | tr -cs 'a-z0-9' '-')
    safe_name=${safe_name#-}
    safe_name=${safe_name%-}
    [[ -n "$safe_name" ]] || safe_name="dir"
    hash=$(printf '%s' "$dir" | md5 | cut -c1-12)
    printf 'ai-sandbox-%s-%s\n' "$safe_name" "$hash"
}

resolve_git_common_dir() {
    local work_dir="$1"
    local git_path="${work_dir}/.git"

    [[ -f "$git_path" ]] || return 0

    /usr/bin/python3 - "$git_path" <<'PY'
import os
import sys

git_path = sys.argv[1]
gitdir = ""

with open(git_path, encoding="utf-8") as fh:
    for line in fh:
        if line.startswith("gitdir: "):
            gitdir = line[len("gitdir: "):].strip()
            break

if not gitdir:
    sys.exit(0)

if not os.path.isabs(gitdir):
    gitdir = os.path.normpath(os.path.join(os.path.dirname(git_path), gitdir))

commondir_path = os.path.join(gitdir, "commondir")
if os.path.isfile(commondir_path):
    with open(commondir_path, encoding="utf-8") as fh:
        commondir = fh.read().strip()
    print(os.path.normpath(os.path.join(gitdir, commondir)))
else:
    print(os.path.normpath(os.path.join(gitdir, "..", "..")))
PY
}

container_exists() {
    local name="$1"
    container list -a 2>/dev/null | awk 'NR>1 {print $1}' | grep -qx "$name"
}

container_running() {
    local name="$1"
    container list 2>/dev/null | awk 'NR>1 {print $1}' | grep -qx "$name"
}

remove_container() {
    local name="$1"

    if container_exists "$name"; then
        log_info "Removing container: $name"
        container rm --force "$name"
    fi
}

ensure_host_mount_sources() {
    local worktrees_dir="$1"

    mkdir -p "$HOME/.config/opencode"
    mkdir -p "$HOME/.cache/opencode"
    mkdir -p "$HOME/.local/share/opencode"
    mkdir -p "$HOME/.local/state/opencode"
    mkdir -p "$HOME/.local/share/mise"
    mkdir -p "$HOME/.cache/mise"
    mkdir -p "$worktrees_dir"

    if [[ -d "$HOME/src/dotfiles" ]]; then
        mkdir -p "$HOME/src/dotfiles/opencode"
    fi
}

append_mount_if_exists() {
    local target_var_name="$1"
    local source="$2"
    local dest="$3"
    local readonly="${4:-}"

    [[ -e "$source" ]] || return 0

    local mount="type=bind,source=${source},target=${dest}"
    if [[ "$readonly" == "readonly" ]]; then
        mount+=",readonly"
    fi

    local quoted_mount
    quoted_mount=$(printf '%q' "$mount")
    eval "$target_var_name+=(\"--mount\" $quoted_mount)"
}

paths_equal() {
    local left="$1"
    local right="$2"

    [[ -d "$left" && -d "$right" ]] || return 1
    [[ "$(cd "$left" && pwd)" == "$(cd "$right" && pwd)" ]]
}

create_container() {
    local work_dir="$1"
    local container_name="$2"

    work_dir=$(cd "$work_dir" && pwd)

    local container_home host_uid host_gid memory cpus
    local git_common_dir worktrees_dir git_name git_email home_dotfiles

    container_home="$HOME"
    host_uid=$(id -u)
    host_gid=$(id -g)
    memory="${AI_SANDBOX_MEMORY:-4G}"
    cpus="${AI_SANDBOX_CPUS:-}"
    git_common_dir=$(resolve_git_common_dir "$work_dir")
    worktrees_dir="$(cd "$work_dir/.." && pwd)/worktrees"
    git_name=$(git config --global user.name 2>/dev/null || true)
    git_email=$(git config --global user.email 2>/dev/null || true)
    home_dotfiles="$HOME/src/dotfiles"

    ensure_host_mount_sources "$worktrees_dir"

    local idle_cmd
    idle_cmd=$(cat <<'EOF'
set -e
trap 'exit 0' INT TERM
while :; do
  sleep 3600 &
  wait $!
done
EOF
)

    local create_args=(
        "--name" "$container_name"
        "--interactive"
        "--tty"
        "--uid" "$host_uid"
        "--gid" "$host_gid"
        "--memory" "$memory"
        "-e" "HOME=${container_home}"
        "-e" "PATH=${container_home}/.opencode/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
        "-e" "OPENCODE_CONFIG=${container_home}/.config/opencode/opencode-yolo.jsonc"
        "-e" "ZDOTDIR=/root"
        "-e" "COLORTERM=truecolor"
        "-e" "GIT_SSH_COMMAND=false"
    )

    if [[ -n "$git_name" ]]; then
        create_args+=("-e" "GIT_AUTHOR_NAME=${git_name}" "-e" "GIT_COMMITTER_NAME=${git_name}")
    fi
    if [[ -n "$git_email" ]]; then
        create_args+=("-e" "GIT_AUTHOR_EMAIL=${git_email}" "-e" "GIT_COMMITTER_EMAIL=${git_email}")
    fi
    if [[ -n "$cpus" ]]; then
        create_args+=("--cpus" "$cpus")
    fi

    append_mount_if_exists create_args "$work_dir" "$work_dir"
    append_mount_if_exists create_args "$worktrees_dir" "$worktrees_dir"
    append_mount_if_exists create_args "$HOME/.config/opencode" "$HOME/.config/opencode"
    append_mount_if_exists create_args "$HOME/.cache/opencode" "$HOME/.cache/opencode"
    append_mount_if_exists create_args "$HOME/.local/share/opencode" "$HOME/.local/share/opencode"
    append_mount_if_exists create_args "$HOME/.local/state/opencode" "$HOME/.local/state/opencode"
    append_mount_if_exists create_args "$HOME/.local/share/mise" "$HOME/.local/share/mise"
    append_mount_if_exists create_args "$HOME/.cache/mise" "$HOME/.cache/mise"
    append_mount_if_exists create_args "$HOME/.agents" "$HOME/.agents" readonly

    if [[ -n "$git_common_dir" ]]; then
        append_mount_if_exists create_args "$git_common_dir" "$git_common_dir"
    fi

    if [[ -d "$home_dotfiles/opencode" ]] && ! paths_equal "$home_dotfiles/opencode" "$work_dir/opencode"; then
        append_mount_if_exists create_args "$home_dotfiles/opencode" "$home_dotfiles/opencode"
    fi

    log_info "Creating sandbox: $container_name"
    container create "${create_args[@]}" "$SANDBOX_IMAGE" /bin/bash -lc "$idle_cmd"
}

ensure_container_running() {
    local work_dir="$1"
    local container_name="$2"

    if ! container_exists "$container_name"; then
        create_container "$work_dir" "$container_name"
    fi

    if ! container_running "$container_name"; then
        log_info "Starting sandbox: $container_name"
        container start "$container_name"
    fi
}

exec_in_sandbox() {
    local work_dir="$1"
    local container_name="$2"
    local mode="$3"
    shift 3
    local -a opencode_args=("$@")

    local prep_cmd
    prep_cmd=$(cat <<'EOF'
: "${XDG_CONFIG_HOME:=$HOME/.config}"
: "${XDG_CACHE_HOME:=$HOME/.cache}"
: "${XDG_DATA_HOME:=$HOME/.local/share}"
: "${XDG_STATE_HOME:=$HOME/.local/state}"
mkdir -p "$HOME" "$XDG_CONFIG_HOME/opencode" "$XDG_CACHE_HOME/opencode" \
  "$XDG_DATA_HOME/opencode" "$XDG_STATE_HOME/opencode"
if [ -f .tool-versions ] || [ -f .mise.toml ] || [ -f mise.toml ]; then
  /usr/local/bin/mise install --yes 2>&1 | tail -5 || true
  eval "$(/usr/local/bin/mise activate bash)" 2>/dev/null || true
fi
EOF
)

    local -a cmd
    case "$mode" in
        shell)
            cmd=(/bin/bash -lc "${prep_cmd}; exec /bin/zsh")
            ;;
        opencode)
            cmd=(/bin/bash -lc "${prep_cmd}; exec \"\$@\"" bash opencode --pure)
            if ((${#opencode_args[@]})); then
                cmd+=("${opencode_args[@]}")
            fi
            ;;
        *)
            log_error "Unsupported exec mode: $mode"
            exit 1
            ;;
    esac

    local -a exec_args=(
        --env "TERM=${TERM:-xterm-256color}"
        --workdir "$work_dir"
    )
    if [[ -t 0 ]]; then
        exec_args=(--interactive --tty "${exec_args[@]}")
    fi

    container exec "${exec_args[@]}" "$container_name" "${cmd[@]}"
}

prune_sandboxes() {
    local containers

    containers=$(container list -a 2>/dev/null | awk 'NR>1 && $1 ~ /^ai-sandbox-/ {print $1}')
    if [[ -n "$containers" ]]; then
        log_info "Removing sandbox containers..."
        while IFS= read -r ctr; do
            [[ -n "$ctr" ]] || continue
            remove_container "$ctr"
        done <<< "$containers"
    else
        log_info "No ai-sandbox containers found"
    fi

    log_success "Prune complete"
}

main() {
    local work_dir="."
    local command="opencode"
    local -a command_args=()

    while [[ $# -gt 0 ]]; do
        case "$1" in
            -h|--help)
                usage
                exit 0
                ;;
            --)
                shift
                command_args=("$@")
                break
                ;;
            build|reset|prune|shell)
                command="$1"
                shift
                command_args=("$@")
                break
                ;;
            *)
                command_args=("$@")
                break
                ;;
        esac
    done

    work_dir=$(cd "$work_dir" && pwd)
    if [[ ! -d "$work_dir" ]]; then
        log_error "Directory does not exist: $work_dir"
        exit 1
    fi

    local container_name
    container_name=$(container_name_from_dir "$work_dir")

    ensure_system_running

    case "$command" in
        build)
            local no_cache=""
            if ((${#command_args[@]})); then
                for arg in "${command_args[@]}"; do
                    if [[ "$arg" == "--no-cache" ]]; then
                        no_cache=true
                    else
                        log_error "Unknown build option: $arg"
                        exit 1
                    fi
                done
            fi
            build_image true "$no_cache"
            ;;
        reset)
            build_image
            remove_container "$container_name"
            log_success "Sandbox reset"
            ;;
        prune)
            prune_sandboxes
            ;;
        shell|opencode)
            build_image
            ensure_container_running "$work_dir" "$container_name"
            exec_in_sandbox "$work_dir" "$container_name" "$command" "${command_args[@]}"
            ;;
        *)
            log_error "Unsupported command: $command"
            exit 1
            ;;
    esac
}

main "$@"
